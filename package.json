{
  "name": "vercel-conversational-doc-fetcher",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "type-check": "tsc --noEmit",
    "test": "vitest run --reporter=verbose tests/unit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:integration": "vitest run --reporter=verbose tests/integration"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@upstash/redis": "^1.37.0",
    "@vercel/blob": "^2.3.2",
    "next": "^16.2.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.0.0",
    "@vitest/coverage-v8": "^4.1.2",
    "dotenv": "^17.3.1",
    "typescript": "^5.4.0",
    "vitest": "^4.1.2"
  }
}
